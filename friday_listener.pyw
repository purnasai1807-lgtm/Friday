"""
FRIDAY AI - Silent Always-On Listener (no console window)
==========================================================
Same as friday_listener.py but uses .pyw extension so it runs
with NO console window. Used for Windows auto-start.
"""
from friday_listener import main

if __name__ == "__main__":
    main()
